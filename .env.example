# --- AI pipeline (required for `npm run eval` and the record flow) ---
ANTHROPIC_API_KEY=        # Claude (note generation). Required for `npm run eval`.
OPENAI_API_KEY=           # Whisper (speech-to-text). Required for live recording.

# Optional model overrides (defaults are sensible)
# CLAUDE_MODEL=claude-sonnet-4-5

# --- Supabase (added in Rung 3; not needed for the AI eval) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- Stripe (added in Rung 2/3) ---
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PRICE_MONTHLY=
NEXT_PUBLIC_STRIPE_PRICE_ANNUAL=

# --- Prototype gate (Rung 1: simple shared password instead of auth) ---
PROTOTYPE_PASSWORD=
